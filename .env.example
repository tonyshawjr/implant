# =============================================================================
# Database Configuration
# =============================================================================
# PostgreSQL connection string for Prisma
DATABASE_URL="postgresql://user:password@localhost:5432/squeezmedía?schema=public"

# Direct URL for Prisma (bypasses pooling for migrations)
DIRECT_URL="postgresql://user:password@localhost:5432/squeezmedía?schema=public"

# =============================================================================
# Facebook/Meta Marketing API
# =============================================================================
# Get these from: https://developers.facebook.com/apps/
# 1. Create a new app or use existing one
# 2. Add "Marketing API" product
# 3. Copy App ID and App Secret from Settings > Basic

FACEBOOK_APP_ID=your_facebook_app_id
FACEBOOK_APP_SECRET=your_facebook_app_secret

# Generate access token at: https://developers.facebook.com/tools/explorer/
# Required permissions:
#   - ads_management (for campaign management)
#   - ads_read (for reading metrics)
#   - business_management (for accessing Business Manager)
# Note: Use a long-lived token or system user token for production
FACEBOOK_ACCESS_TOKEN=your_facebook_access_token

# =============================================================================
# Google Ads API
# =============================================================================
# 1. Apply for API access: https://ads.google.com/aw/apicenter
# 2. Create OAuth credentials: https://console.cloud.google.com/apis/credentials
# 3. Enable Google Ads API in Cloud Console

# Developer token from Google Ads API Center (Basic or Standard access)
GOOGLE_ADS_DEVELOPER_TOKEN=your_developer_token

# OAuth 2.0 Client ID and Secret from Google Cloud Console
GOOGLE_ADS_CLIENT_ID=your_client_id.apps.googleusercontent.com
GOOGLE_ADS_CLIENT_SECRET=your_client_secret

# OAuth 2.0 Refresh Token
# Generate using the OAuth 2.0 Playground or your own auth flow
# Scopes required: https://www.googleapis.com/auth/adwords
GOOGLE_ADS_REFRESH_TOKEN=your_refresh_token

# Optional: Manager (MCC) Account ID for accessing multiple client accounts
# Format: 123-456-7890 (without dashes when used in API)
GOOGLE_ADS_LOGIN_CUSTOMER_ID=1234567890

# =============================================================================
# Sync & Cron Configuration
# =============================================================================
# Secret for authenticating Vercel Cron jobs
# Generate a random string: openssl rand -hex 32
CRON_SECRET=your_cron_secret_here

# Optional: API key for manual sync triggers via API
# Generate a random string: openssl rand -hex 32
SYNC_API_KEY=your_sync_api_key_here

# =============================================================================
# Additional Configuration (for future phases)
# =============================================================================

# Stripe (Billing)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_PUBLISHABLE_KEY=pk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# Twilio (SMS/Voice)
# TWILIO_ACCOUNT_SID=AC...
# TWILIO_AUTH_TOKEN=...
# TWILIO_PHONE_NUMBER=+1...

# OpenAI (AI Features)
# OPENAI_API_KEY=sk-...

# Anthropic Claude (AI Features)
# ANTHROPIC_API_KEY=sk-ant-...

# Mapbox (Territory Maps)
# MAPBOX_ACCESS_TOKEN=pk...

# SendGrid (Email)
# SENDGRID_API_KEY=SG...

# =============================================================================
# Seed Configuration (Development Only)
# =============================================================================
# Password for seeded users during development
# If not set, a secure random password will be generated
# Minimum 8 characters required
# SEED_PASSWORD=your_secure_dev_password
