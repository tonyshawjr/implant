/**
 * Constants Module
 *
 * Central export point for all application constants.
 * Import from this module for cleaner imports.
 *
 * @example
 * ```typescript
 * import { INTERNAL_ROLES, isClientRole, Role } from '$lib/constants';
 * ```
 */

export * from './roles';
